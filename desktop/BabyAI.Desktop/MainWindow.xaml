<Window
    x:Class="BabyAI.Desktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="Transparent">
        <Button x:Name="OrbButton"
                Width="112"
                Height="112"
                CornerRadius="56"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Click="OrbButton_Click"
                Padding="0"
                BorderThickness="1">
            <Grid>
                <Ellipse x:Name="OrbCore" Width="88" Height="88" Opacity="0.92">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="#FFF7F9FF" Offset="0" />
                            <GradientStop Color="#FFB8C8FF" Offset="0.55" />
                            <GradientStop Color="#FF6D7CFF" Offset="1" />
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
                <Ellipse Width="54" Height="54" StrokeThickness="2" Stroke="#BFFFFFFF" />
                <TextBlock x:Name="StateGlyph"
                           Text="•"
                           FontSize="34"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="White" />
            </Grid>
        </Button>
    </Grid>
</Window>
