<Window
    x:Class="BabyAI.Desktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid x:Name="Root" Background="Transparent" Padding="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition x:Name="PanelColumn" Width="0" />
        </Grid.ColumnDefinitions>

        <Button x:Name="OrbButton"
                Grid.Column="0"
                Width="112"
                Height="112"
                CornerRadius="56"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Click="OrbButton_Click"
                PointerPressed="OrbButton_PointerPressed"
                PointerMoved="OrbButton_PointerMoved"
                PointerReleased="OrbButton_PointerReleased"
                Padding="0"
                BorderThickness="1">
            <Grid>
                <Ellipse x:Name="OrbCore" Width="88" Height="88" Opacity="0.92">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="#FFF7F9FF" Offset="0" />
                            <GradientStop Color="#FFB8C8FF" Offset="0.55" />
                            <GradientStop Color="#FF6D7CFF" Offset="1" />
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>
                <Ellipse Width="54" Height="54" StrokeThickness="2" Stroke="#BFFFFFFF" />
                <TextBlock x:Name="StateGlyph"
                           Text="•"
                           FontSize="34"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="White" />
            </Grid>
        </Button>

        <Border x:Name="Panel"
                Grid.Column="1"
                Width="0"
                Height="420"
                Margin="12,0,0,0"
                CornerRadius="28"
                Background="#A8171B27"
                BorderBrush="#42FFFFFF"
                BorderThickness="1"
                Opacity="0"
                Visibility="Collapsed">
            <Border.Transitions>
                <TransitionCollection>
                    <RepositionThemeTransition />
                </TransitionCollection>
            </Border.Transitions>
            <Grid Padding="20" RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Spacing="2">
                    <TextBlock x:Name="IdentityText" Text="BabyAI" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock x:Name="TaskText" Text="No active task" Opacity="0.65" TextWrapping="Wrap" />
                </StackPanel>

                <Border Grid.Row="1" Background="#22FFFFFF" CornerRadius="16" Padding="12">
                    <TextBlock x:Name="ReplyText" Text="Ready." TextWrapping="Wrap" />
                </Border>

                <TextBox x:Name="MessageBox"
                         Grid.Row="3"
                         PlaceholderText="Напиши BabyAI..."
                         AcceptsReturn="True"
                         MinHeight="72"
                         TextWrapping="Wrap" />

                <Grid Grid.Row="4" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Send" Click="SendButton_Click" />
                    <Button x:Name="ApproveButton" Grid.Column="1" Content="Approve" Click="ApproveButton_Click" Visibility="Collapsed" />
                    <Button x:Name="RejectButton" Grid.Column="2" Content="Reject" Click="RejectButton_Click" Visibility="Collapsed" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
